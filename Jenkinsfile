pipeline {
    agent any
    
    environment {
        NODE_ENV = 'test'
    }
    
    stages {
        stage('Build') {
            steps {
                // Your build steps here
            }
        }
        // Add more stages as needed
    }
}
